# Main Lead 5.0 — T024 ready, held for tomorrow

**Superseding transfer/routing:** The user requested Main Lead 6.0. Read [the latest amendment](main-lead-6.0.md) before dispatch: use the user-corrected exact `opencode-go/muse-spark-1.3-contributor#high` route; DeepSeek V4.1 Flash is the recorded fallback, and Opus is temporarily unavailable. Technical scope/gates below remain intact; the old same-lead prompt and earlier free-route preference are historical.

User-authorized lead: **GPT-6 Astra / Main Lead 5.0**, task
`01a0ccc7-6219-73c1-bd3b-1521bb71a837`.

The user requested notification when T024 is reached and said to pick it up tomorrow.
**T024 is ready to start, but has not been dispatched or implemented. Stop here until
the user resumes.** T025 has not run. This is a source checkpoint, not runtime,
portable, human, customer or main acceptance.

## Exact pickup

- Lead branch: `codex/bridge-ticket-integration`, draft [PR84](https://github.com/yugant99/IpManagement/pull/84).
- Lead worktree: `/Users/yuganthareshsoni/.codex/worktrees/ipam-bridge-ticket-integration/Ip_inventory`.
- Complete T021/T022/T023 source assembly: **`e6b76edaa9ae5b82a0c409fec151bf52168036bc`**.
- T021 offline API: **`3007bf33dd3481f1b85723b0a1c4971036b5f089`**, [PR96](https://github.com/yugant99/IpManagement/pull/96), Opus 5.5 high.
- T022 recipient preparation: **`c231663ef9a9747c83288523da9ede93977bef42`**, [PR95](https://github.com/yugant99/IpManagement/pull/95), Muse 1.3 high.
- T023 stopped recovery: **`adb2dc18fe747d4ff97a90f1a21bcdc4382f9fea`**, [PR92](https://github.com/yugant99/IpManagement/pull/92), Muse 1.3 high.
- All three have independent GPT-6 Sol high source closure after original-author
  corrections. See `delivery/offline-api-source-review.md`,
  `delivery/recipient-preparation-source-review.md` and `delivery/state-recovery-source-review.md`.
- Application assembly remains `a2b567b670129671f85498729753c4d577d4768e`;
  subsequent completed slices are preparation documents. Schema is 7.

The publication commit containing this record adds the review/registry handoff to
that assembly. On resume, refresh Git/PR/worker state, verify the assembly is included,
and pin one exact current publication commit before creating the T024 worker. Do
not restart planning, switch another checkout or advance the frozen T012/T013 base.

## T024 complete assignment on resume

Author: **Muse 1.3 high in OpenCode**, exact
`opencode-go/muse-spark-1.3-contributor#high`; independent full-result reviewer:
**GPT-6 Sol high**. Astra owns integration and acceptance. Use a new isolated
`codex/` branch/worktree; original authors retain fixes. Freshly verify exact route,
variant and actual usage before dispatch; preserve the 20% reserve. Existing paid
training consent permits only the tracked public synthetic repository, never
private documents, credentials or untracked outputs. No capacity purchases,
resets, top-ups, account changes or silent model substitutions.

Nine-path lease, recorded in `delivery/operator-preparation-ownership.md`:

1. `scripts/ops/acquire.sh`
2. `scripts/ops/health.sh`
3. `scripts/ops/README.md`
4. `specs/001-postmeeting-bridge/delivery/operator-handoff.md`
5. `Dockerfile`
6. `compose.yaml`
7. `docs/RUNNING.md` — bridge auth/readiness/config text only.
8. `scripts/ops/start.sh` — readiness/actual-volume comments and messages only.
9. `scripts/ops/backup.sh` — readiness/actual-volume comments and messages only.

The final three paths remove stale documentation/log claims; do not change their
operational flow or add an ops framework/common helper. This is one complete
operator/package feature assignment, not a micro-test.

Required source behavior and handoff:

- Acquisition uses a separately provisioned coordinator token file, bootstrap
  without domain, trusted coordinator identity and configuration pins, then manual
  `POST /api/schedule/run` with the original stable key/reason. No automatic retry,
  replacement key or guessed identity; transport loss remains unknown.
- Readiness uses a separate domain Operator token file, explicit domain, bootstrap
  and pins, then `/api/readiness`. Success requires HTTP200 and all six exact boolean
  fields true: `process_ready`, `schema_ready`, `data_ready`, `static_ready`,
  `configuration_ready`, `domain_state_compatible`. Docker `/healthz` remains minimal
  anonymous process liveness, not readiness or business recovery.
- Tokens never enter argv, URLs, trace/log output, Compose environment, images,
  snapshots or browser persistence. Use protected input/descriptor/config handling;
  loopback only, no redirects. Preserve explicit revoked/stale/unknown outcomes.
- Compose mounts separately provisioned reviewed `IPAM_ACCESS_CONFIG` read-only and
  **requires explicit `IPAM_DATA_VOLUME`**. New candidate means a new recorded
  disposable volume; reuse requires deliberate recorded intent. Compose project
  naming alone does not isolate a globally named volume. Preserve local storage,
  loopback binding, read-only root and single process. No new infrastructure.
- Pin source/package/UI/feed/config/schema references, stopped backup/restore and
  paired SQLite/`.recovery.json` transfer, changed-config/unverified classification,
  separate readiness/business comparison, dependencies/licences and presenter path.
  Declare unknowns; no actual version/build/host/license-clearance claims from source.

Read the current API reference, recipient pack/config example and frozen recovery
wire before editing. Retain the accepted configured-recipient policy: one reviewed
Operator per domain/scope, immutable per-notice-version binding, explicit in-app
receipt; descriptive owner reference never establishes business-owner signoff.

## Remaining gates and people

No tests or test additions, builds, imports, typechecks, lint, SQL/DB execution,
servers, app browser checks or runtime probes before the prerequisite-complete
T025 gate. The current user hold prevents T024/T025 tonight. Resuming T024 does not
itself waive or pass any T025 prerequisite or start runtime work. The controlling user instruction already names the authorized T025 gate and
limits it to disposable synthetic data. That existing scoped authority applies only
after the exact complete candidate and prerequisites have source closure; it does
not permit earlier routine checks or broader runtime work. Activity outside that
scope needs new permission. Source preparation alone never passes T025.

An actual recipient/portable target remains unavailable and human acknowledgement
unobserved. That does not prevent later authorized source work or local technical
T025 on its own pinned disposable target. Required business-state comparison is
performed and must pass at T025. Actual human evidence belongs to T026 or an honest
pending disposition. Pending human rehearsal alone does not block a validated
technical-package merge; it limits the human-handoff claim.

Next bridge-to-main merge remains **T028**, after exact-candidate T025, closed
blocking reviews, final Sol T027 and recorded human/business dispositions.
`main` last refreshed at `04eba98cb8673406d1e5d38c5318fb963cc77ff7`; PR79 remains
draft and not merge-ready. No deployment, customer access, questionnaire promotion
or Tier B work follows. **Tier B locked.**

The user confirmed Spencer never received the prepared assignment; agents completed
T020/T022 and own future T024. No message or invented task for tomorrow was sent to
Spencer. His old prepared worktree remains untouched. Retained Luna T014/T016 workers
keep correction ownership; other user tasks/checkouts are independent.

## Copyable resume prompt

> Continue Main Lead 5.0 from the T024-ready checkpoint in this handoff. The stop
> before T024 is now lifted. Do not restart planning. Refresh Git/PR/worker state
> and exact model routes/usage, pin one source base containing the accepted assembly
> and latest handoff, and dispatch the complete nine-path T024 operator/package
> feature to Muse 1.3 high in a new isolated codex branch/worktree. Astra remains
> lead and GPT-6 Sol high independently reviews the full implementation and shared
> invariants before integration. Preserve all recorded authority, evidence, token,
> volume, no-spend and Tier B boundaries. No routine application checks before the
> authorized prerequisite-complete T025 gate; no source-only main merge.
