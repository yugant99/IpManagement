# Bounded audit response: F1–F7

Status: **authorized implementation; new runtime acceptance pending**. Main Lead 2.0
`01a0bb80-cf0d-7f60-8e46-1e825f42d276` retains leadership. This is a follow-up within
the existing worker structure, not Stage 6 or a new architecture exercise.
Starting main, freshly checked against GitHub, is
`a279f32df0ac7d2147b580dbff36dd88772bdeb2`.

The user explicitly requested substantive F1–F7 implementation, connected review,
focused local checks and one final integrated rehearsal. The immediate target is
a reliable local Mac demonstration. Spencer retains Part 6 packaging and
operator/recipient handoff.

## Authority and preservation

- F1 includes independently approved local intended-inventory correction. This
  supersedes G23's deferral only for bounded synthetic registration; general
  reclamation, live provisioning and a configurable workflow engine stay excluded.
- Focused tests, necessary builds, disposable localhost/browser/API checks,
  migration/state-preservation checks and rehearsal are authorized. Use new
  synthetic stores or copies. Preserve retained acceptance stores/artifacts;
  do not repeat unaffected passing checks for reassurance.
- No cloud/VM/Docker execution, image pulls, public exposure, spending or paid
  external-model calls. Portable execution needs separate authorization and
  observations. Packaging source/documentation stays in Spencer's lane.
- Preserve untracked `audit/` and ignored `assessment/`. Publish only sanitized
  generic requirements and synthetic behavior; no customer extracts, attachments
  or databases.
- Historical Stage 5 remains **14 passes, two partial cases S5-09/S5-15, no observed
  application defect within its exercised scope**. New results are an addendum.
- PR #9 stays OPEN / NO MERGE. Historical accounting remains **29 Demonstrated /
  24 Partial / 9 Documentary / 49 Missing = 111** until new observations are
  adjudicated. Neither customer phase is complete.

## Ownership and dependencies

| Owner | Assigned follow-up | File boundary |
|---|---|---|
| Main Lead 2.0 | Contracts, independent review, integration, F6 claims and F7 accounting | Global docs, private assessment, final acceptance |
| Stage 3, `01a0b8f0-7036-74d1-81c1-e388d17b4cf4` | F1 correction, F2 rule, F3 history, F5 workflow | `inventory_commands.py`, `rules.py`, `calculations.py`, `workflow.py`, `reconciliation.py`, `reports.py`, owned checks/handoff |
| Stage 4, `01a0bacd-a541-73e1-8a06-f674dd4a1bcd` | Sole schema/migration owner, shared API/UI, scheduler/feed integration | Schema SQL, `store.py`, `app.py`, shared frontend, owned integration handoff |
| Foundation 1, `01a0b8ae-bd65-7331-b548-da5dca1e0d5e` | State/CLI migration compatibility and preservation | `state_ops.py`, `__main__.py`, state docs/checks; no competing schema edits |
| Overseer/data, `01a0b8b9-82fb-7a11-bc50-ec3a5b234729` | Synthetic episode and discrepancy-identity advice | Fixtures/feed ownership; original bytes/geometry frozen |
| Execute Stage 5 acceptance handoff, `01a0baff-2143-7073-ad3d-4963a9cc0ca5` | Focused acceptance, missing cases, demo state and final rehearsal | New `docs/evidence/audit-response/`, `docs/handoffs/stage-05-audit-response.md`, `docs/DEMO_RUNBOOK.md` |
| Spencer | Package checkpoint against final candidate, eventual operator/recipient handoff | Existing Part 6 paths unchanged; absence of registration is a visibility gap |

Retain other workers for their assigned fixes. Use isolated `codex/` feature
branches, explicit dependencies and coherent commits; push after three changes or
earlier for handoff. Shared migrations are serialized through Stage 4. Workers do
not self-merge or declare global completion. The lead records the exact shared
contract before dependent implementation.

## Required outcomes

| Finding | Required result and evidence boundary |
|---|---|
| F1 | Exact top-level prefix proposal inside existing managed scope; no pending inventory mutation; independent authorized decision with scope/overlap/version rechecks; atomic correction/decision/audit and safe retries; rejection leaves inventory unchanged. Link a subsequent stored reconciliation result, accounting for every ghost in the chosen cycle. New prefix policy may remain unknown. Keep original fixtures/feed compatibility and allocation behavior. |
| F2 | `metadata_gap` for missing owner/purpose, with evaluated metadata/version/provenance and healthy controls. Audited edit plus rerun produces evidence-based resolution; old runs unchanged. Update new totals, not historical evidence. |
| F3 | Metadata edits preserve eligible history when geometry/capacity are unchanged. Supported structural changes visibly invalidate affected own/ancestor pools. Keep concurrency guards. Do not claim current occupancy/positive leases disappear or saved runs are erased. |
| F5 | Preserve original finding and audit; show latest evidence separately. Explicit close/reopen policy; healthy comparable evidence alone establishes resolution. Recurrence/materially new discrepancy renews notice; unchanged anomaly does not. Stable identity ignores generated IDs and normal cycle time changes. Ownership, acknowledgement, handoff, escalation and retries stay coherent. |
| F4 | Dedicated native state, exact startup, pinned runs/cycles/preset, controlled scheduler and stopped-service backup/recovery. Independent operator follows instructions. One final integrated rehearsal covers correction, metadata, allocation, uncertainty and recovery. Portable recipient acceptance is separate. |
| F6 | Presenter actions map to real UI/API and retained evidence. Distinguish fixed rule thresholds from configurable acquisition interval; CSV/JSON exports from full assessment; real local correction from simulated provisioning; evidence resolution from workflow state; synthetic from customer discovery. |
| F7 | Reassess affected original clauses using observations. No automatic RFP-037/072 downgrade. Keep 15 catalog scenarios, 16 Stage 5 cases and 111 questionnaire requirements separate; preserve IPv6 delegated-record and customer-phase gaps. |

## Focused acceptance and finish

Close the missing active ordinary reconciliation versus timer, actual OS shutdown
during acquisition, prior-success preservation under asset refusal, and
first-path-specific refusal observations on disposable data/processes. Account
for in-process asset caching and SQLite writers blocking failure-audit persistence;
record actual outcomes rather than predicted audit assertions.

Final handoff: F1–F7 dispositions, reviewed/merged SHAs and PRs, checks actually run,
catalog and questionnaire adjudication, startup/recovery instructions, unresolved
defects and portable recipient gaps. Time, tokens and available agents are not
evidence of completion.
