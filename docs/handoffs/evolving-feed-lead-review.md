# Evolving-feed source review

Date: 2026-09-19. Persistent lead: `01a0b845-6c8d-7021-a5c9-15e673db07c9`.

[PR #19](https://github.com/yugant99/IpManagement/pull/19), **`3218daade8de3fe61bd6df36da431411add4a3ad`**, branch `codex/part-2-evolving-feed`, is accepted as an **unmerged source checkpoint only**. Exact base is Stage 3 `c2629fbf6516a744e65c52a424700563c0f4a0a2`. The existing data owner remains responsible; its worktree is `Ip_inventory-synthetic-data`. Five changed files stay inside fixtures/data-document ownership. Frozen v1 files and original generator are unchanged in this diff.

The lead read the complete 172-line producer, its recipe, changed-file scope and current importer/time interfaces. An independent bounded source reviewer inspected interval/history generation against original v1 data. No actionable finding: inputs are copied; original interval IDs/times are retained; renewals start at actual expiry; new episodes use deterministic identities and bounded rolling intervals; policy snapshots advance with scenario time; documented North partial and Central stale views preserve unknown states. No expected-answer manifest is consumed by the producer. Historical conflicts and p95 are not promised to clear with current departures.

Interface: normal Python package `ipam_synthetic_feed` under `fixtures/evolving/`, pure `build_cycle(index, baseline_envelopes)`, returning `feed_version`, `cycle_id`, `cycle_index`, `demo_clock_at`, and policy plus eight observation envelopes. Cycle 0 is reference; first scheduler advancement is 1. Maximum 1460 gives explicit exhaustion after 365 simulated days. Source-run IDs advance with timestamp-bearing content, logical rich source IDs remain stable. The adapter must pin original baseline assets: header/shape checks alone do not prove immutable input. It must distinguish documented declared-incomplete coverage from unexpected rejected records without consuming the expected-answer file.

No function was called, imported, parsed by an interpreter, generated or serialized. No test/build/runtime/state/infrastructure command was run. Counts/size bounds in worker docs are source arithmetic, not measured output. Actual importer compatibility, generated bytes/counts, cycle results and callable package availability remain unverified. Scheduler, asset loading, wheel inclusion, schema/clock transaction, timer/manual replay and UI are not implemented by this PR.

The lead approves normal history-preserving combination of this **exact** source checkpoint with Stage 3 publication `63287e0ff281b678abbd8809ca9160739de8c537` and current lead documentation on the separate Stage 4 feature branch. Do not merge application PRs into main, substitute later moving feed heads or consume expected labels. Core and Spencer ownership stays unchanged. Denominator 111; no demonstrated-row credit, runtime acceptance or `PART6_READY` change.
