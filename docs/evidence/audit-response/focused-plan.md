# Focused checks for the agreed F1–F7 response

This is an acceptance plan, not passed evidence. The lead's `AUDIT_RESPONSE_CONTRACT.md` controls shared behavior. Stage 3 owns inventory/rule/workflow code; Stage 4 owns schema/shared integration/frontend; Foundation owns explicit state-command compatibility. Stage 5 reuses their sufficient focused results, fills actual gaps and performs one final connected rehearsal after review.

## F1 — correction approval and real reconciliation

- Propose a missing top-level prefix from an actual anomalous saved ghost/managed-route finding, with owner, purpose, reason and reviewed inventory version. Pending/rejected requests leave inventory unchanged.
- Independently approve/reject; exercise self/unauthorized/stale, scope/perimeter/overlap and changed-keyed-payload refusals. Approval must atomically persist prefix, decision and before/after audit. Exact request/decision retries add no effects. Reuse one owner atomic-failure proof if adequate.
- Record original finding, first subsequent comparable result and latest inspection. Approval alone is not resolution. Newest unknown/incomparable evidence cannot fall back to an older healthy outcome.
- At cycle 1 register Central `10.80.240.0/24` for actual ghost `10.80.240.10`, reconcile and inspect healthy comparable evidence. At cycle 6 the new `10.80.243.10` episode needs its own `10.80.243.0/24` registration before whole-perimeter resolution. If both are uncorrected at cycle 6, a subset correction must remain anomalous. Separate `10.80.241.0/24` unregistered-route evidence from DHCP ghosts.
- Confirm new prefixes retain unknown route-policy results and the next actual acquisition remains compatible. Do not modify original fixture geometry or weaken authority checks. Existing two-actor allocation still works.

## F2 — metadata evidence

- Rich baseline has one explained `metadata_gap` anomaly at Lab `10.40.15.0/24` (`a1526294-9c3c-5588-98b5-c0fa7cae2aa9`), with 59 healthy metadata controls before new prefixes. Observe totals; expected numbers never feed detection.
- Retain evaluated blank values, prefix/ledger versions and original/current audit provenance; original seed provenance must not falsely attest edited values.
- Audited metadata correction plus ordinary reconciliation yields healthy comparable evidence and `resolved_by_evidence`; old saved runs/exports remain immutable.

## F3 — history validity and concurrency

- On unchanged source selection/scenario time, change owner, purpose, tags and custom fields through supported inventory APIs. Compare capacity/current occupancy, all 720 samples, p95 eligibility/value, daily p95, slope and forecast. Metadata/version/provenance fields may change; numeric history must stay eligible and equal.
- Preserve existing prefix/pool/ledger concurrency bumps and exercise stale inventory and allocation review guards. A separate history token must not remove those checks.
- On a separate structural control, create/resize a supported empty descendant under a pool-bearing ancestor. Confirm the UI's `edit_context.history_impact.structural` warning names the actually affected pool and the new run visibly withholds unsupported history. Unaffected pools remain eligible; metadata edits do not revalidate already-invalid history.
- Do not invent a public pool-range editor or attempt prohibited attached-pool bounds changes as the supported action. Current occupancy can remain available; positive leases can disprove zombie criteria; old saved runs remain intact.
- Foundation covers conservative migration of legacy pool/prefix versions. Enumerate actual finalized tables/columns rather than assuming historical schema counts.

## F5 — evidence, lifecycle and renewed notification

- Show original immutable finding/run and latest comparable finding separately. North cycle 4 anomaly → cycle 5 unknown cannot resolve; cycle 6 healthy can establish evidence resolution. Missing/incomparable/not-applicable controls must stay unknown.
- Execute acknowledgement, escalation, handoff, recipient acknowledgement, healthy close and explicit reopen with actual versions/reasons. Record owner/audit effects and unauthorized/stale refusals. Check immediate exact replay including handoff by the original actor after ownership changes.
- Observe Central new-member/recurrence notice at cycle 6 after earlier `.240` correction. Reuse owner focused proof of unchanged anomaly across generated record IDs (cycles 1→2) and North recurrence at cycle 12 if adequate; do not repeatedly run a broad scenario suite.
- Stable discrepancy keys must ignore generated run/record IDs, renewals, ordering and wording; new material members renew once, unchanged/subset anomalies do not. Reconciliation and manual workflow state are separate.

## F4/F6/F7 — connected rehearsal and evidence handoff

Use a new exact-candidate environment, newly compiled UI and dedicated disabled demo store. Prepare cycle 1, a pinned report and a stopped initial snapshot; demonstrate corrections, metadata, two-actor allocation, exception actions, North 4→5→6 and Central cycle-6 recurrence, then deliberate cycle-7 uncertainty. Retain a distinct final evidence snapshot before restoring the initial state. Confirm all actual tables and selected run/preset/clock/cursor/replay identities survive recovery.

A separate agent may follow the completed runbook on the same Mac. Label this agent procedural reproducibility; human presenter/recipient transfer remains unobserved unless actually performed. Spencer's exact package checkpoint and portable recipient gate stay separate.

Every presenter step must identify an actual UI/API action and retained evidence. Keep configurable scheduling distinct from fixed thresholds; findings CSV/run JSON/audit CSV distinct from a customer assessment report; local corrections/allocations distinct from simulated external provisioning; and synthetic observations distinct from customer discovery.

Lead adjudicates catalog scenarios 11/14 and affected questionnaire clauses from observations. Preserve RFP-037/RFP-072 historical classifications pending clause-specific review; do not automatically downgrade them. F5 escalation supplies new evidence for RFP-082 but does not establish external paging or adopted operating processes. No aggregate completion percentage combines documentary/partial/demonstrated/missing classes.
