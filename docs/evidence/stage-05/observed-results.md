# Stage 5 observed local results — 2026-09-19

Application pickup **`117d05295473cf25d362adb320e6fef26ecdd74c`**, accepted code **`54f8f8168108733d40d842263218f16b33df5868`**. No application correction was made or substituted. Execution followed user authorization published at `c4cccef8b2014c7bcb2740b9f26a683f3e9d26c6`, incorporated by documentation-only merge `e7a84f32ab2f409493a205a7f8357d39831f8245`.

All relative artifact paths below resolve under **`/Users/yuganthareshsoni/Downloads/Ip_inventory-stage-5-artifacts/run-20260919-O8RVgx`**. These files are retained locally, not committed databases/logs. [run-summary.json](run-summary.json) records compact identities and selected artifact hashes. Synthetic scenario clocks, actual wall time and controlled scheduler time are separate.

## 1. Installation and readiness — S5-01/02

Observed macOS 14.5 (23F79), arm64, Python 3.12.10, uv 0.7.13, npm 10.9.2. Existing Node 23.11 was outside the declared engine range; acceptance installed `node@22.14.0` only under `node-runtime/` and used that binary. No dependency locks changed. `UV_PROJECT_ENVIRONMENT=<root>/environment UV_CACHE_DIR=<root>/uv-cache uv sync --frozen`, `npm --prefix frontend ci --cache <root>/npm-cache --no-audit --no-fund`, and the single `npm --prefix frontend run build` completed successfully. The latter runs TypeScript and Vite 8.3.0. Commands ran in the isolated Stage 5 checkout with the isolated Node directory prepended to PATH.

Explicit rich seed from the unchanged `fixtures/v1/inventory.json` created four scopes, 60 prefixes, eight pools and one allocation. Single-worker CLI service at `127.0.0.1:8765`, PID 69330, used `rich-scheduler/`, candidate `frontend/dist` and immutable `fixtures/v1`. `/healthz` returned ready/schema 4/data/static ready; compiled UI rendered the API inventory. Separate empty `setup-control/` at port 8767, PID 69329, returned HTTP 503 `SETUP_NEEDED` and rendered setup instructions. After stop, seed succeeded once and repeated seed refused `ALREADY_INITIALIZED`. Active primary seed and backup refused `DATA_IN_USE`.

Evidence: `logs/{uv-sync,npm-ci,frontend-build,rich-seed,primary-service,setup-service}.log`, `node-install.log`, `primary/environment-inputs.json`, `primary/{ready,setup-needed,setup-api-refusal,setup-stopped-seed-1,setup-stopped-seed-2}.json`, `primary/active-lock-{seed,backup}`, `primary/{seeded-ui,setup-ui}.{txt,png}`. Setup and primary CLI shutdowns completed with exit 0.

## 2. Acquisition and browser recovery — S5-03/04

First manual POST used actor `demo-approver`, reason `Stage 5 first acquisition`, key `stage5-g2-cycle1`. HTTP 201 created operation `dad97a28-2bc9-41c2-9757-5d27387e4534`, run `e6d41d3f-7adb-4812-8467-142226dce591`, cycle `evolving-v1-000001`, nine distinct accepted receipt batches, scenario time `2026-09-01T06:00:00.000Z`, wall completion `2026-09-19T19:52:39.455Z`. Each receipt had accepted=input and zero rejected/duplicate rows. Exact replay returned HTTP 200 and the same identities; all 15 captured application tables were equal. Changed reason with the same key returned 409 `IDEMPOTENCY_CONFLICT`, preserving acquisition effects and adding one explicit failure audit.

An acceptance-only loopback proxy at port 8769 forwarded the browser's cycle-2 request to the real backend, recorded its committed response, then substituted HTTP 503 once. The UI visibly retained key `e489318c-dae3-422d-82b6-c114b7768bd0` through navigation and same-tab reload. “Retry exact Run now request” recovered operation `1d0511db-2c2a-4498-9218-902395aaa976`, run `87a27db7-691b-4c60-a7c7-1628756558e3`, the same nine batches, scenario `2026-09-01T12:00:00.000Z` and wall completion `2026-09-19T19:53:42.855Z`. Exact forwarded payloads match; all 15 application tables before/after recovery match. This is controlled response substitution, not a spontaneous network failure or browser-storage-denial test.

Evidence: `primary/{cycle1-summary,browser-recovery-summary}.json`, exact `cycle1*.json`, `proxy-posts.jsonl`, `response_proxy.py`, `schedule-response-lost.{txt,png}`, `schedule-retained-{navigation,reload}.txt`, `schedule-recovered.{txt,png}`. Table equality excludes SQLite internal tables and file bytes.

## 3. Findings, arithmetic and pinned reports — S5-05/06/07

The separate source-control store imported nine original envelopes, then explicitly created baseline run `46ce8b8f-511f-4d1e-83ce-dc0945580192` at scenario `2026-09-01T00:00:00.000Z`. Independent code read raw lease intervals and intended ranges; it did not call application calculation helpers or load expected-outcome manifests. All eight capacities, seven 720-hour histories, nearest-rank p95s, complete-day p95s, OLS slopes and applicable forecast outputs matched the saved run (58 recorded comparisons). Capacity/current/p95 examples: North DHCP 245/150/150; Coastal growth 100/69/68 with 31 days; Lab conflict pool 100/61/60 with forecast beyond horizon; North static capacity 13 with occupancy/forecast not applicable.

Scoped North/Lab `10.40.1.10` reuse remained non-conflicting. Lab `.100` had two incompatible overlapping clients but counted as one occupied address. Static DHCP controls produced healthy baseline, anomalous complete/partial positive claims, unknown partial silence, then healthy complete silence. Static pressure/oversized/zombie remained not applicable. Anomaly-to-unknown comparison reported `resolution_unknown`.

Primary manual cycles 3–7 saved nine batches and one run each. Cycle 5 correctly returned partial acquisition; cycle 7 returned complete acquisition while Central occupancy/history/forecast remained unavailable due to stale source observations. North prefix `41aca2b0-ec1d-4f64-8d21-c5af0ab0b002`, rule `missing_expected_route`, was anomalous at cycle 4, unknown at 5 and healthy at 6. Comparisons 4→5 and 4→6 returned `resolution_unknown` and `resolved_by_evidence`. Saved-run fields and JSON exports agreed; cycle 1 remained unchanged after cycle 7.

| Cycle | Run ID | Synthetic clock | Acquisition outcome |
|---|---|---|---|
| 3 | `5c7f5c1f-7343-4452-b380-ccf9e0c5ede0` | `2026-09-01T18:00:00.000Z` | complete |
| 4 | `94ee61d7-8722-44d2-90e5-1a29f320cee5` | `2026-09-02T00:00:00.000Z` | complete |
| 5 | `0b861518-7ae1-4b92-a1fb-47179f0731d2` | `2026-09-02T06:00:00.000Z` | partial |
| 6 | `8534644a-f7a0-4c4c-8dd3-eee6592d08c6` | `2026-09-02T12:00:00.000Z` | complete |
| 7 | `1d9443cc-2671-4f2e-8bb3-b910a2dd5143` | `2026-09-02T18:00:00.000Z` | complete |

The rendered cycle-7 Central cards showed stale/unavailable; expanded Central access 1 JSON exactly equaled its saved calculation. The UI later selected/pinned cycle 1 and saved a preset. Its CSV had 113 findings × eight columns, all cells matching the pinned run, with matching `X-Run-ID`/`X-Preset-Revision`; the UI reported the download requested. Updating the preset caused its prior revision to return 409 `STALE_REPORT_PRESET`. Final preset revision is `7d9a5a3af7dfcd9b66b84f8108d3ae9c15212d9764d666f065a6a539cedf5ec3`.

Evidence: `source-control/{source-control-report.md,observations.json,independent-arithmetic.json,metadata.json}`, numbered HTTP records and derived synthetic inputs; `primary/evolution-summary.json`, `cycle{3..7}-saved.json`, matching exports/comparisons, `older-cycle1-after-cycle7.json`; `primary/preset-summary.json`, `preset-csv.json`, `report-cycle7-{dashboard.txt,dashboard.png,central-detail.txt}`, `report-preset-{saved-ui,export-ui}.txt`. The earlier `report-cycle1-selected-later.txt` is a loading snapshot and is **not** proof of cycle-1 selection.

## 4. Configuration, timer and rollback — S5-08/09/10

Ordinary HTTP configuration saved six-hour enabled config 2, then cycle 3 rescheduled due exactly six hours after wall completion. One-hour config 3, disable 4, re-enable 5 and disable 6 behaved as expected. Old config and requester saves returned `STALE_SCHEDULE`/`FORBIDDEN`. Manual cycles ran while disabled. Primary later saved config 7 disabled/null due for preservation.

The separate scheduler harness ran actual application route handlers, transactions, scheduler threads and FastAPI lifespans in process. Only `scheduler._now` was controlled, beginning `2030-01-01T00:00:00.000Z`; no host clock or production interval changed. No real one-hour/six-hour wait or HTTP transport is claimed for this harness.

- A held acquisition made manual acquisition and the ordinary-run handler return `RUN_IN_PROGRESS`; only the winning acquisition committed. A due timer against the harness-held actual ordinary-run guard recorded busy/future due. This represents lock occupancy; it did not execute an active ordinary reconciliation during that timer attempt.
- An old six-hour failure handler installed its old retry tuple after new one-hour config 3 was saved. At the new due time, a cycle committed and moved due one hour forward. Raw field `after_old_failure_before_new` is a **post-save config-3 snapshot**, despite its label.
- Restart after advancing controlled wall time 48 hours, placing it 42 hours beyond the saved due time, committed once with a future six-hour due; another wake at the same instant did not catch up further. This restarted the existing store; it did not back up/restore an enabled snapshot.
- Direct scheduler stop waited for held timer work, joined its thread, and retained the data lock until lifespan exit. This does not cover an OS termination signal during acquisition or a process crash.
- One injected exception after nine imports, one saved run and ten new exceptions rolled back clock/import/run/queue/cursor/replay and successful effects. Exactly one separate failure audit persisted. The same key then succeeded and replayed. This does not cover commit/fsync/failure-audit-loss branches.

Evidence: `primary/evolution.py`, `schedule-after-cycle3.json`, config HTTP records; `scheduler-harness/{REPORT.md,acceptance.py,observations.json,assertions-summary.json,execution.log,artifact-sha256.json}`. Eight bounded harness records passed, with limits above. S5-09 remains **PARTIAL** against the full prepared wording; controlled observations passed, while active ordinary reconciliation versus timer and OS-signal-during-acquisition remain unrun.

## 5. Inventory and workflow — S5-11/12/13

API created IPv4 child `0bac88a1-34fe-4be8-ae07-27de28f96b7a` at `10.40.250.0/24`, resized it to `/25`, and persisted metadata/version 2. Fresh-version overlap `10.40.1.0/24` returned 409 `PREFIX_OVERLAP`, changing only one failure audit. IPv6 preview reported 65,536 total / two blocked / 65,534 free `/64` children; saving a returned result created `a5b0c2f9-42d6-4b6c-a08b-459d1960fd5e`, `2001:db8:40::/64`. Prefix total became 62, baseline version 4. Original fixed feed geometry stayed intact; parent versions legitimately advanced. Rendered search for `acceptance` returned precisely the two new records.

Browser request `b363e946-8584-4eb8-900d-654d4a0cece4` used requester Mira and fresh pool 1/ledger 4. Different actor Rowan approved it, creating allocation `452cb304-a4ae-4e65-85f4-b1db4b90486b` at `10.40.2.3`, pool version 2/ledger 5. Local allocation is real; downstream status is explicitly `simulated_success`. The acceptance proxy separately replaced each committed creation/decision response with text/plain HTTP 503, producing `INVALID_RESPONSE`. Each UI exact retry recovered the same payload/effect; all 15 captured application tables were unchanged on each retry. Two success audit IDs: `abb71bc7-d2d7-44a6-8a3c-621ab32b9b1c`, `34315752-42fe-4756-a5cf-00719cfaa396`. Workflow retry was observed in the mounted UI, with no reload-persistence claim. Changed creation payload with its retained key returned 409.

Separate source-control guards refused requester decision, approver self-approval, stale review and a current fresh DHCP contradiction imported after pending creation and before any rerun. Four failure audits persisted; no new allocation resulted.

Exception `9b38f0a7-58a3-4d51-ab4c-039fd8de9e55` transferred from Mira/Access Planning to Rowan/Network Operations, then Rowan acknowledged it at `2026-09-19T20:10:55.465Z`, version 3, notification cleared. The queue UI displayed the recipient and acknowledged state. Original finding/run objects remained pinned; only exceptions and audit tables changed. This is an actual fixed-demo-team transfer, not external collaboration integration.

Evidence: `primary/{inventory-summary,workflow-recovery-summary,exception-summary}.json`, matching HTTP/snapshots/proxy payloads, `inventory-filter.{txt,png}`, `workflow-{create,decision}-{uncertain,recovered}.txt`, `exception-acknowledged.{txt,png}`; negative cases in `source-control/observations.json`.

## 6. Preservation, refusals and migration — S5-14/15/16

S5-14 passed 20 bounded observations. The post-disable baseline in `primary/preservation-baseline-after-disable.json` contains config 7 disabled/null due/not in progress; 62 prefixes, two allocations, one request, 38 audits, seven runs, 11 exceptions, one preset, 63 batches, seven schedule operations, four scopes, eight pools, 84 coverage rows and 17,718 source records. Primary was stopped before backup to `snapshots/primary-populated.sqlite3`. All 15 application tables matched by canonical row-content hashes and counts after backup, fresh restore, restored restart, retained-key replay and final copy shutdown.

Only the restored copy advanced to cycle 8, scenario `2026-09-03T00:00:00.000Z`, operation `25c082b0-f51b-4c23-b907-aeac13fd0776`, run `928d80ba-192b-4cf4-8e7f-ac4518b376a1`. Restoring the original snapshot over it recovered the baseline and preserved the entire newer state as `state-operations/ipam_demo.before-restore-20260919T201623-9f05235601df43bb89e4d438d0ccd15c.sqlite3`. Restart remained disabled/null due. The retained cycle-1 key returned its original operation/run with HTTP 200/replay true and no table changes; post-backup key `stage5-preservation-postbackup-cycle8` was absent and was not resubmitted. Primary stayed unchanged. Evidence: `preservation/{preservation-report.md,summary.json,observations.json,processes.json}`, numbered command/HTTP records and per-table digests. Copy services 73322 and 73367 stopped orderly, exit 0. Comparison covers full column values, including saved JSON, rather than counts alone.

S5-15 invalid receipt explicitly reported nine input rows = one accepted + seven rejected + one duplicate, with partial application status/effective completeness false. Separate copied assets/stores refused missing assets and byte-altered policy (`SYNTHETIC_FEED_UNAVAILABLE`), accepted foreign evidence then refused scheduled acquisition, and refused a changed rich geometry even when copied seed/current rows matched (`SYNTHETIC_FEED_INCOMPATIBLE`). Refused acquisitions changed only failure audit/status. These asset/authority cases preserved fresh initial state; preservation of a prior advanced successful cycle under those failures was not exercised. **PARTIAL** against the full S5-15 wording; first-path-specific refusal also remains unrun.

S5-16 used genuine historical schema-v3 code from `git archive 009e80197ab610e5a365d1e3dd4462588754d646`, populated with rich inventory, nine imports, one run, approved allocation and preset. Current backup/restore retained v3 and returned `migration_required:true`. Actual startup returned 503 `UNSUPPORTED_SCHEMA` without mutation. Explicit migration preserved every original row/ID and added disabled six-hour config 1/cursor 0/null due/zero operations. Correctly configured v4 startup returned ready/eligible and preserved state. Retained counts include nine batches, 2,260 records, one run, one request, two allocations, 22 audits, ten exceptions and one preset. This is **OBSERVED_PASS for representative v3 only**; v1/v2 and customer legacy-data migration are unrun.

Evidence: `source-control/observations.json`, `scheduler-harness/REPORT.md`, `legacy/{README.md,result.json,provenance.json,artifacts.sha256}`, exact legacy command/HTTP/semantic comparisons. Legacy services at port 18768 were stopped with exit 0.

## Observer corrections and unrun limits

No application defect was found in the exercised scope. Observer issues are retained transparently: source-control omitted a new directory before its first seed; evolution initially selected the wrong rule label after cycles already succeeded; exception comparison initially used a nonexistent table name after successful handoff; preset read initially used an incorrect artifact filename. These were corrected in the observation layer without repeating committed operations. Legacy logging, occupied-port and feed-path corrections are documented in its report. Preservation also corrected a restart-only socket preflight after an observed address-in-use error with no listener; no successful backup/mutation/restore was repeated. TIME_WAIT is only an unconfirmed explanation in both lanes. One browser label locator timed out before mutation and was replaced with the visible combobox locator. A transient report loading snapshot was excluded in favor of later completed UI evidence. Raw failed logs were not discarded.

Unrun: reset; v1/v2 migration; enabled-snapshot backup/restore; active ordinary reconciliation while timer is due; OS signal during acquisition; cycle-1460 exhaustion; exhaustive cycles; first-path-specific authority refusal; prior-success preservation under asset refusal; unavailable-asset committed replay; failure-audit loss; SQLite commit, filesystem copy/replace/fsync/WAL/crash branches; browser storage denial; scale/endurance/HA; Docker/Compose/Linux/recipient startup. No live discovery, traffic measurement, safe release/reclaim, SSO or provider capability follows from these synthetic observations.
