# T027 final independent review — GPT-6 Sol

**Reviewer:** visible Codex GPT-6 Sol task `01a0cf9a-ce88-7f90-8556-1fa4b131bf26`  
**Disposition:** no blocking source or evidence finding remains for the bounded local synthetic Tier A technical package after the T025 evidence correction at `75f213ea8934cb275a083724c1dc4d3ce79d2c42`. T028 acceptance and merge remain the lead's decisions.  
**Application candidate:** `8eb3e231b7792fe8415455d2176e962b8f953d60`  
**Lead assembly checkout:** `4362624ac2a7008ac3d6cd91abb248cd77d77f0d` (`8eb3e23..4362624` changes documentation only).  
**Verifier packet reviewed:** PR #104, initial `0d1f605ac409e56a6f6195a8f9f837af815160f2`, evidence-pointer correction `75f213ea8934cb275a083724c1dc4d3ce79d2c42`, `specs/001-postmeeting-bridge/delivery/independent-evidence.md` and `evidence/t025/`.  
**Private synthetic run root:** `/Users/yuganthareshsoni/Downloads/Ip_inventory-t025-artifacts/run-20260923-lead6` (metadata and sanitized observations only; no bearer, configuration body, database, or full export copied here).

## Decision and evidence correction

1. **No source blocker observed.** The exact candidate keeps server-side bearer authority, selected-domain filtering, current configuration pins and independent actor decisions. Creation/replay, reservation conversion/release, ticket uncertainty/readback, recipient-version acknowledgement, canonical assessment/signoff, and stopped restore follow their stated bounded contracts in the inspected paths. Existing focused source-review receipts remain applicable; this review checked their shared invariants against the frozen runtime packet, rather than treating earlier source acceptance as runtime evidence.
2. **Evidence pointer finding closed at `75f213e` (non-code).** The initial `evidence/t025/wrapper-security.json` reported `health_bad_mode = 2` but its manifest-bound `candidate4/logs/wrapper-negative-matrix.json` entry actually refused a non-loopback URL before token validation. The T025 owner separated the two cases: `health_bad_mode_non_loopback_url` points to that URL-first refusal, while `health_bad_mode_valid_loopback` points to `candidate4/logs/health-wrapper-bad-mode.json`. The new manifest entry binds the latter at 300 bytes and SHA-256 `9a739dba9839d64b7b6d39cf13d162b047230dff36fdfc5750473fa5cc5366c1`. I independently read the retained artifact and matched its size/hash; it records exit 2 at token-file validation after a valid loopback URL. The correction changes only the sanitized summary and manifest, with no new runtime run or application source change.
3. **T025 state is still lead-pending.** Its criterion matrix reports PASS for recorded cases, while the packet header and checkpoint explicitly say in progress pending final review/adjudication. T027 does not convert those observations into global acceptance. T026 human rehearsal remains pending/not run; Linux `amd64` recipient portability and production/customer behavior are unobserved. T019 commercial-role assignment remains documentary/unassigned. Tier B remains locked.

## Independent trace

| Boundary | Source and observation checked | Disposition |
|---|---|---|
| Authority, scope, replay | `access.py` reloads reviewed configuration per request, authenticates the bearer, binds one permitted domain, and returns scoped 404 for foreign resources. `app.py` reauthenticates inside `BEGIN IMMEDIATE`; coordinator preflight uses a short read transaction, and global routes require coordinator authority. Runtime packet records operator global denial, foreign detail/export denial, stale pins, exact acquisition replay, changed-key conflict and response-loss recovery. | Bounded case evidence supports the contract. |
| Import and assessment | Canonical staged candidate and parser receipt feed `migration_compare.py`; stable identities and accepted/active-only rows remain separate. Signoff checks independent principal, exact digest/version, current baseline and configuration, zero rejected/conflicting/duplicate rows, both count equations and explicit active-only acknowledgement. Observed 55 accepted rows reconcile as `55=0+0+55+0`; one active-only row is separate; malformed, duplicate, mixed-domain and over-limit inputs were refused without active writes. | No contradiction found. |
| Reservation and capacity | `workflow.current_static_occupancy` counts local allocations plus `state='reserved'` holds, including expired holds. Conversion inserts the allocation and transitions the matching hold under one writer, so observed occupancy remains 3 while components change 1 allocation/2 holds to 2 allocations/1 hold. `lifecycle._release_eligibility` checks allocations and pending/routing-blocked/unknown linked ticket intents at proposal and decision. Observed approved unused release restored remaining capacity 4→5; pending and unknown linked cases returned `EXTERNAL_EFFECT_UNRESOLVED` and stayed reserved. One timed two-writer race created one hold and refused the other as stale. | No double count or silent release observed. |
| Ticket uncertainty | `ticket_handoff` durably reserves each attempt/ordinal before an effect, retains exact correlation and payload digest, and fences an absent uncertain attempt on readback. A found effect returns its persisted synthetic ticket ID; a lost response stays unknown until readback. Recorded success/replay/conflict, committed-effect loss, definitive absence, three-attempt cap and disabled connector behavior agree with this model. | No false delivery claim found. |
| Notice recipients | `lifecycle` binds an immutable notification version to the reviewed current recipient and checks both bound and current identity on acknowledgement. Evaluation renews for alarm, route change or legacy binding, while GET does not mutate. Observed v1–v7 history, combined alarm/route single bump, old-recipient read-only access, exact receipt replay and changed-reason conflict. | In-app receipt is correctly narrower than a human handoff. |
| Saved versus current metrics | `workflow.current_static_occupancy` labels current local intended-ledger counts separately from saved run metrics. Browser packet shows authenticated saved-run detail/export and Unknown presentation for incomplete or stale DHCP; 12 Central findings and four prior anomalies remained unknown. | No saved/current or lease/traffic conflation found in checked paths. |
| Stopped recovery | `state_ops` validates standalone input, paired manifest/hash/schema, source stability and candidate integrity before replacement, preserves prior data, and classifies like-for-like/changed/unavailable/legacy-absent configuration separately. The final populated before/after recovery snapshots match across all 32 tables (28 nonzero), with six readiness booleans and selected-domain read after restore. Timed local clone: restore 1.191 s, readiness at +0.737 s, business read at 1.931 s; preprovisioned code, UI, config, dependencies and feed are outside that timer. Malformed schema-6 acknowledgement refused migration without changed bytes. | Supports only the declared disposable stopped-local boundary. |
| Wrapper and UI continuity | `acquire.sh`/`health.sh` now use valid protected curl-config long names, numeric-loopback URLs, protected token-file checks, bounded request capture and explicit unknown handling. Candidate2 `996d0a8` had a complete successful `npm ci` and build; its frontend tree, `package-lock.json`, `uv.lock` and `pyproject.toml` identities match candidate4, and the served compiled assets are byte-pinned. Candidate1 failed full TypeScript build and its complete transcript was not retained; candidate3's timed-out acquire remained unknown and was not counted as success. | Wrapper cases are scoped to recorded negatives; the bad-mode artifact binding was corrected at `75f213e`. |

## Verification boundary and next gate

I reviewed source, sanitized response summaries, the manifest and referenced private artifact bytes/hashes; all 87 initial manifest references checked had no missing file or hash mismatch, and the newly added 88th private-artifact reference matched at correction review. I also compared the final **populated** pre/post-restore table-hash snapshots directly; the earlier preliminary empty-history snapshots are not the populated recovery proof. I did not run tests, builds, API calls, database commands or new runtime cases, per this review lease and repository rules. The observations are synthetic on loopback and do not establish a recipient Linux run, production readiness, customer use or actual human acknowledgement.

The evidence pointer correction is closed. The lead can adjudicate T025 and this T027 review for the local technical package. T026 and commercial/recipient gates must retain their explicit pending dispositions at T028; no worker should mark global completion or merge from this review alone.
